heya
DONT touch the badeline stuff (like if youre mapping they just crash for now)

feel free to snip around and also change stuff

you should use frost helper's shader compiler, you can get it  from here:
https://github.com/JaThePlayer/FrostHelper/wiki/Shader-Starter-Guide

parsing for the title banner shader (map metadata) are this:

"
Title:
  Effect: EffectName
  Parameters:
  - paramName: name
    paramType: type (limited support for now)
    paramValue: value
TitleAccent:
  Effect: EffectName
  Parameters:
  - paramName: name
    paramType: type (limited support for now)
    paramValue: value
"